export enum CustomSize {
  sm = "sm",
  md = "md",
  lg = "lg",
}

export enum CustomColor {
  default = "default",
  accent = "accent",
  success = "success",
  warning = "warning",
  danger = "danger",
}
