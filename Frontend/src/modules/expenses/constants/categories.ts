/**
 * Suggested categories for the autocomplete — a starting point, not a fixed list. `category` is
 * stored as plain text on the backend, so typing anything not in this list works exactly the
 * same as picking one of these.
 */
export const EXPENSE_CATEGORY_PRESETS = ["Tea & Refreshments", "Food & Breakfast", "Travel & Transport", "Stationery & Office Supplies", "Maintenance & Repairs", "Miscellaneous"] as const
